{
  "$schema": "https://railway.app/v2.schema.json",
  "build": {
    "builder": "Nixpacks",
    "buildCommand": "npm install --prefix backend && npm run postinstall --prefix backend"
  },
  "deploy": {
    "startCommand": "npm start --prefix backend",
    "restartPolicyType": "ON_FAILURE"
  }
}
